[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "gap2-nightly-probe"
version = "0.1.0"
license = {text = "MIT"}
description = "Minimal test package for prompt injection security validation"
requires-python = ">=3.8"
repository = "https://github.com/9-nine-noa/gap2-nightly-probe"
